{
  "name": "orbe-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Web/PWA de Orbe. index.src.html es la fuente editable; `npm run build` genera index.html (sin @babel/standalone en runtime).",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
